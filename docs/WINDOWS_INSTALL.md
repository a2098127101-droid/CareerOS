# Windows Install

## Fast path

1. Extract the ZIP outside OneDrive-synced folders when possible.
2. Double-click `OPEN_CareerOS.cmd`.
3. On first run, allow the local environment to initialize.
4. Later use the generated CareerOS desktop shortcut.

If the full runtime cannot initialize, the launcher can open the standalone H5 Showcase.
