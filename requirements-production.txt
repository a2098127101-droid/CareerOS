-r requirements.txt
# Non-demo production additionally requires externally provisioned PostgreSQL+pgvector, Redis and S3-compatible storage.
# Runtime code fails closed when distributed dependencies are required but not configured.
