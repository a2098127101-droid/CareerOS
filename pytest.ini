[pytest]
pythonpath = .
# The execution image injects a global ddtrace pytest plugin that can hang interpreter teardown
# after SQLAlchemy tests. CareerOS does not depend on that plugin.
addopts = -p no:ddtrace
